# Mutation to add multiple lines to checkout at once
mutation CheckoutAddLines($id: ID!, $lines: [CheckoutLineInput!]!, $languageCode: LanguageCodeEnum!) {
	checkoutLinesAdd(id: $id, lines: $lines) {
		checkout {
			id
			lines {
				id
				quantity
				variant {
					id
					name
					translation(languageCode: $languageCode) { name }
					product {
						name
						translation(languageCode: $languageCode) { name }
						slug
					}
				}
				totalPrice {
					gross {
						amount
						currency
					}
				}
			}
			totalPrice {
				gross {
					amount
					currency
				}
			}
		}
		errors {
			field
			message
			code
		}
	}
}

