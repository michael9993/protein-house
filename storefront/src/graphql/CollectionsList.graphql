# Fetch collections for the products page carousel
query CollectionsList($channel: String!, $first: Int = 6) {
  collections(channel: $channel, first: $first) {
    edges {
      node {
        id
        name
        slug
        description
        backgroundImage {
          url
          alt
        }
        products(first: 4) {
          totalCount
          edges {
            node {
              id
              name
              slug
              thumbnail {
                url
                alt
              }
              media {
                url
                alt
              }
            }
          }
        }
      }
    }
  }
}

