query ProductsForSizes($channel: String!, $first: Int!) {
  products(first: $first, channel: $channel) {
    edges {
      cursor
      node {
        id
        name
        attributes {
          attribute {
            id
            name
            slug
          }
          values {
            id
            name
            slug
          }
        }
      }
    }
  }
}

