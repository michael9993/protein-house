query CheckoutFind($id: ID!) {
	checkout(id: $id) {
		id
		email
		# User ownership data for isolation checks
		user {
			id
			email
		}
		# Channel for validation
		channel {
			slug
		}
		lines {
			id
			quantity
			totalPrice {
				gross {
					amount
					currency
				}
			}
			variant {
				product {
					id
					name
					slug
					thumbnail {
						url
						alt
					}
					category {
						name
					}
				}
				pricing {
					price {
						gross {
							amount
							currency
						}
					}
				}
				name
				id
			}
		}
		totalPrice {
			gross {
				amount
				currency
			}
		}
	}
}
