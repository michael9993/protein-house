query ProductListByCategory($slug: String!, $channel: String!, $first: Int!) {
	category(slug: $slug) {
		name
		description
		seoDescription
		seoTitle
		products(first: $first, channel: $channel) {
			edges {
				node {
					...ProductListItem
				}
			}
		}
	}
}
