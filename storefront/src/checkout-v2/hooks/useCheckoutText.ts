"use client";

// Re-export from the shared location at @/lib/checkout/.
export {
	useCheckoutText,
	CheckoutTextProvider,
	formatText,
	type CheckoutTextConfig,
} from "@/lib/checkout/useCheckoutText";
